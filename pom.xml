<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <groupId>com.ruwei</groupId>
    <artifactId>isegoria-cloud</artifactId>
    <version>1.0.0-SNAPSHOT</version>
    <packaging>pom</packaging>
    <name>isegoria-parent</name>
    <description>IsegoriaForum 微服务聚合工程</description>


    <modules>
        <module>isegoria-common</module>
        <module>isegoria-model</module>
        <module>isegoria-client</module>
        <module>isegoria-user</module>
        <module>isegoria-post</module>
        <module>isegoria-interaction</module>
        <module>isegoria-social</module>
        <module>isegoria-notify</module>
        <module>isegoria-rec</module>
    </modules>

    <properties>
        <dubbo.version>3.3.0</dubbo.version>
        <java.version>17</java.version>
        <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
        <project.reporting.outputEncoding>UTF-8</project.reporting.outputEncoding>
        <spring-boot.version>3.5.3</spring-boot.version>
        <mybatis-plus.version>3.5.17</mybatis-plus.version>
        <sa-token.version>1.41.0</sa-token.version>
        <hutool.version>5.8.37</hutool.version>
        <knife4j.version>4.5.0</knife4j.version>
        <cos.version>5.6.227</cos.version>
        <springdoc.version>2.8.10</springdoc.version>
        <nacos-client.version>2.4.2</nacos-client.version>
    </properties>

    <dependencyManagement>
        <dependencies>
            <!-- Spring Boot 官方 BOM：web/validation/websocket/redis/es/amqp/test 等全部由此管版本 -->
            <dependency>
                <groupId>org.springframework.boot</groupId>
                <artifactId>spring-boot-dependencies</artifactId>
                <version>${spring-boot.version}</version>
                <type>pom</type>
                <scope>import</scope>
            </dependency>

            <!-- Sa-Token BOM：锁定所有 sa-token-* 模块为同一版本。
                 1.41.0 起修复 JDK 17 下 redis-jackson 序列化会话时的反射兼容问题（InaccessibleObjectException） -->
            <dependency>
                <groupId>cn.dev33</groupId>
                <artifactId>sa-token-dependencies</artifactId>
                <version>${sa-token.version}</version>
                <type>pom</type>
                <scope>import</scope>
            </dependency>

            <!-- Dubbo BOM：tri 协议 + Nacos 注册中心相关依赖统一版本 -->
            <dependency>
                <groupId>org.apache.dubbo</groupId>
                <artifactId>dubbo-bom</artifactId>
                <version>${dubbo.version}</version>
                <type>pom</type>
                <scope>import</scope>
            </dependency>

            <!-- MyBatis-Plus：jsqlparser 需显式依赖（starter 未传递该模块，分页必需） -->
            <dependency>
                <groupId>com.baomidou</groupId>
                <artifactId>mybatis-plus-spring-boot3-starter</artifactId>
                <version>${mybatis-plus.version}</version>
            </dependency>
            <dependency>
                <groupId>com.baomidou</groupId>
                <artifactId>mybatis-plus-jsqlparser-4.9</artifactId>
                <version>${mybatis-plus.version}</version>
            </dependency>

            <dependency>
                <groupId>cn.hutool</groupId>
                <artifactId>hutool-all</artifactId>
                <version>${hutool.version}</version>
            </dependency>

            <dependency>
                <groupId>com.github.xiaoymin</groupId>
                <artifactId>knife4j-openapi3-jakarta-spring-boot-starter</artifactId>
                <version>${knife4j.version}</version>
            </dependency>

            <dependency>
                <groupId>com.qcloud</groupId>
                <artifactId>cos_api</artifactId>
                <version>${cos.version}</version>
            </dependency>

            <!-- ===== 工程内模块（P0：公共组件单模块，包内按 core/web/mybatis 分层） ===== -->
            <dependency>
                <groupId>com.ruwei</groupId>
                <artifactId>isegoria-common</artifactId>
                <version>${project.version}</version>
            </dependency>
            <dependency>
                <groupId>com.ruwei</groupId>
                <artifactId>isegoria-model</artifactId>
                <version>${project.version}</version>
            </dependency>
            <dependency>
                <groupId>com.ruwei</groupId>
                <artifactId>isegoria-client</artifactId>
                <version>${project.version}</version>
            </dependency>
        </dependencies>
    </dependencyManagement>

    <build>
        <pluginManagement>
            <plugins>
                <plugin>
                    <groupId>org.apache.maven.plugins</groupId>
                    <artifactId>maven-compiler-plugin</artifactId>
                    <version>3.8.1</version>
                    <configuration>
                        <source>17</source>
                        <target>17</target>
                        <encoding>UTF-8</encoding>
                    </configuration>
                </plugin>
                <plugin>
                    <groupId>org.springframework.boot</groupId>
                    <artifactId>spring-boot-maven-plugin</artifactId>
                    <version>${spring-boot.version}</version>
                </plugin>
            </plugins>
        </pluginManagement>
    </build>
</project>
