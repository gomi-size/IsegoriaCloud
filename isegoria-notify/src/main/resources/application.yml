# 应用服务 WEB 访问端口（notify 服务：8205）
# 完整配置段（MySQL / Redis / RabbitMQ / Dubbo / Sa-Token / MyBatis-Plus / springdoc / cors；
# WebSocket 端点 /api/ws 由 Phase 7 的 WebSocketConfig 注册，见 docs/phase2-六服务骨架.md §5.4）
server:
  port: 8205
